# On Cloudflare, configuration lives in wrangler.jsonc (vars + d1_databases),
# not in this file. This is only a fallback for plain `next dev`.

# Name shown in the app header and returned by /api/health
APP_NAME="Link Shortener"
